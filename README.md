# repo-audits workflow

The `repo-audits` repository enables the creation of Pull Request for packages that need to have a `fnd` role in their `DESCRIPTION`. 

Packages that are tagged with a particular grant number will be audited and a PR will be created if the `fun` person / role is
not in the `DESCRIPTION` file. 

